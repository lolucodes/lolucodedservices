export default class Account {
    constructor(userId, studentId, hasOutstandingBalance, id) {
        this.userId = userId;
        this.studentId = studentId;
        this.hasOutStandingBalance = hasOutstandingBalance;
        this.id = id;
    }

}
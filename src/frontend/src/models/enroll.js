export default class Enroll {
    constructor(userId, courseId, title, fee, enrollmentTime, id) {
        this.userId = userId;
        this.courseId = courseId;
        this.title = title;
        this.price = fee;
        this.enrollmentTime = enrollmentTime;
        this.id = id;

    }
}